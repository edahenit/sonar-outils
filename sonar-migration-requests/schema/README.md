# Request schema — informational copy

This file is a copy of
`sonar-migration-engine/src/migration/schema/demande.schema.json`, provided
here only for IDE auto-completion and a local pre-commit check. **It is
not authoritative**: validation executed by the privileged pipeline
always loads the schema from the engine repository, never from this
repository.

See the monorepo's root README, § "deviation from the prompt: schema and
inventory in the engine repository", for the security justification.

If this file diverges from the engine repository, only the latter
counts; update this copy to avoid IDE confusion, but any drift between
them has no effect on the pipeline's behavior.
