---
# Expected variables (supplied by the calling role via 'vars:' of an
# include_role — see sonar_export and sonar_import):
#   sonar_ce_task_url_base : e.g. "{{ sonar_url }}/api/ce/task"
#   sonar_ce_task_id        : task identifier returned by the call that
#                              triggered the export/import
#   sonar_ce_token          : admin token of the instance concerned
#
# These are common (group_vars/all.yml) and reused as-is:
#   sonar_ce_intervalle_secondes, sonar_ce_timeout_secondes, sonar_ce_tentatives
