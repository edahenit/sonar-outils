[defaults]
inventory = inventaire/depuis_instances.py
host_key_checking = True
retry_files_enabled = False
stdout_callback = yaml
# No secret in the logs: tasks that handle a token explicitly carry
# 'no_log: true' in every role, but default verbosity is also reduced here.
display_args_to_stdout = False

[ssh_connection]
pipelining = True
